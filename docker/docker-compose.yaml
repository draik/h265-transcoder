---

services:
  converter:
    image: draikx21/h265-converter
    restart: unless-stopped
    user: 1000:1000
    environment:
      BATCH: 0
      CONVERT: "True"
      DEBUG: "False"
      DELETE: "False"
      RETRY_FAILED: "False"
      TZ: "UTC"
    volumes:
      - mnt:/mnt
